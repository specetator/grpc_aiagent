<network-security-config>
    <!-- Development emulator bridge only; production should use HTTPS/WSS. -->
    <domain-config cleartextTrafficPermitted="true"><domain includeSubdomains="false">10.0.2.2</domain></domain-config>
</network-security-config>
